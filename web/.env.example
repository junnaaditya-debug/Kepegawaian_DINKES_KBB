# Base URL of the deployed Worker API (e.g. https://simpeg-dinkes-kbb-api.<subdomain>.workers.dev
# or a custom domain like https://api.simpeg-dinkes-kbb.example.go.id).
# Leave unset for local development — Vite proxies /api to http://localhost:8787 (see vite.config.ts).
VITE_API_BASE_URL=
