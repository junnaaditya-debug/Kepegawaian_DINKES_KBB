# Leave unset for local development (Vite proxies /api to the Worker on :8787).
# In production, set this to the deployed Worker's URL, e.g.:
# VITE_API_BASE_URL=https://simpeg-dinkes-kbb-api.<your-subdomain>.workers.dev/api
VITE_API_BASE_URL=
